#!/bin/sh

dirname "`readlink -f "${BASH_SOURCE[0]}"`" | read bin

[[ "$PATH" =~ "$bin" ]] || PATH="$PATH:$bin"

alias mediasync="sudo -u mediacat \"$bin/catalog\" -nolisten -sync ."
alias briefmedia='brief=1 findmedia . '

